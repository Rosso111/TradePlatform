[pytest]
markers =
    unit: Unit-Tests (keine DB, keine HTTP)
    integration: Integrationstests (DB + HTTP)
    system: Systemtests (vollständige User Flows)
testpaths = tests
addopts = -v --tb=short
